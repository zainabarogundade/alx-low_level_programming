#include "main.h"

/**
 * print_alphabet - prints the alphabet in lowercase, followed by a new line.
 *
 * Return: Always 0.
 */
void print_alphabet_x10(void)
{
	char letter;

	for(letter = 'a'; lette <= 'z'; letter; letter++)
		_putchar(letter);

	_putchar('\n');
}
